{
  "_comment": "Ephemeral MCP config for Stage 1 (/spec-architect). Load ONLY for the spec session with: claude --mcp-config ~/.claude/spec-mcp.json  — copy this file there once and fill the placeholders. It is NOT persisted to user/project config, so it costs zero tokens in other sessions. Lean, read-only advisory servers only.",
  "mcpServers": {
    "aws-knowledge": {
      "type": "http",
      "url": "https://knowledge-mcp.global.api.aws"
    },
    "well-architected": {
      "command": "uvx",
      "args": ["awslabs.well-architected-security-mcp-server@latest"],
      "env": {
        "AWS_PROFILE": "<your-aws-profile>",
        "AWS_REGION": "<your-aws-region>"
      }
    },
    "aws-pricing": {
      "command": "uvx",
      "args": ["awslabs.aws-pricing-mcp-server@latest"],
      "env": {
        "AWS_PROFILE": "<your-aws-profile>",
        "AWS_REGION": "us-east-1"
      }
    }
  }
}
